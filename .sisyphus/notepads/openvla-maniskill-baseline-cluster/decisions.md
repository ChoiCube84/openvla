
- Locked task scope to one defaults/contract module (`experiments/robot/maniskill/defaults.py`) plus `.gitignore` update; no runner/preflight/launcher logic added.
- Fixed canonical benchmark task IDs to exactly `PickCube-v1`, `PushCube-v1`, `StackCube-v1` and exemplar limits to `{"success": 2, "failure": 2}` for stable downstream imports.
- Encoded FailSafe paper unknowns as explicit `ASSUMPTIONS` entries (version, episode count, seed set, checkpoint provenance) to avoid presenting unverified details as facts.
- Set checkpoint and seed policies as explicit defaults/assumptions so later orchestration can override while preserving a clear baseline contract.
- Corrected phase-1 defaults: full seed list now `[7]`, frame contract root uses `frames`, and added `ASSUMPTION_LEDGER_PATH_TEMPLATE` for downstream artifact wiring.
- Kept setup/runtimes scripts standalone-importable under `experiments/robot/maniskill/` with no new dependencies and no benchmark runner logic.
- Ordered `check_setup.py` validations so explicit checkpoint misconfiguration (`OPENVLA_MANISKILL_CHECKPOINT=/tmp/does-not-exist`) deterministically returns `CHECKPOINT_MISSING:` before later GPU/dependency checks.
- Disk budget check is bound to `ARTIFACT_ROOT` + `RAW_FRAME_LAYOUT_CONTRACT["retention"]`, with optional override via `OPENVLA_MANISKILL_MIN_FREE_DISK_BYTES` and a conservative default floor when unset.
- Updated GPU policy check to treat `CUDA_VISIBLE_DEVICES` as physical-GPU selection: when present it must include `3` and expose at least one CUDA device; when absent it falls back to requiring direct visibility of physical GPU index 3.
- Added `experiments/robot/maniskill/artifacts.py` as the single artifact utility module for task 3; avoided adding helper modules to keep imports stable for upcoming runner/rebake tasks.
- Kept frame path contract at `frames/<task>/episode_<index>/` (no `raw_frames` path) and stored exact episode frame directory in each JSONL episode record for deterministic rebake references.
- Made video dependency soft at import-time by deferring `imageio` import to `save_video_from_frames()` so metadata helpers remain usable in environments missing renderer packages.
- Standardized absent exemplar class representation as explicit per-task counters in exemplar metadata rather than implicit inference from missing arrays.
- Tightened artifact contract compliance in `artifacts.py`: frame directories now include `seed_<seed>` (`frames/<task>/seed_<seed>/episode_<index>/`) and per-task exemplar selection now sorts by `episode_index` before picking first success/failure exemplars.
- Implemented task-id gating in runner as strict allowlist against `defaults.TASK_IDS`; unsupported values exit non-zero with `UNSUPPORTED_TASK:` before evaluation starts.
- Kept ManiSkill helper module intentionally narrow (`maniskill_utils.py`): env creation, image extraction, action adaptation, and termination/success interpretation only.
- Chose artifact helper APIs (`create_run_layout`, `build_episode_metadata`, `append_episode_record`, `write_manifest`, `write_summary`) as the only persistence path for run outputs; no ad-hoc summary/episode file layouts were introduced.
- Defaulted video writing off (`save_videos=False`) to respect benchmark scope and avoid per-episode MP4 generation in this task.
- Tightened task-4 runner contract by making ManiSkill env creation explicit (`obs_mode=rgb`, `control_mode=pd_ee_delta_pose`) with enforced 7D action-space validation and task-specific unnorm-key resolution against model `norm_stats`.
- Aligned draccus entrypoint signature to repo convention (`def eval_maniskill(cfg: ManiSkillEvalConfig)`) by removing default dataclass instance to avoid decorator type resolution errors.
- Removed postponed-annotation mode from the runner module so draccus receives concrete runtime type hints for `ManiSkillEvalConfig` during entrypoint inspection.
- Implemented task-5 rebake as a standalone argparse CLI (`rebake_videos.py`) using artifact helper contracts only (`build_exemplar_manifest`, `get_video_path`, `save_video_from_frames`) to keep scope small and avoid benchmark/runtime coupling.
- Locked failure signaling for deterministic automation: missing run directory prints `RUN_DIR_MISSING:<path>` and missing/empty frame directory prints `FRAME_DIR_MISSING:<path>` before exiting non-zero.
- Set rebake default to non-destructive output handling: existing MP4s are skipped unless `--overwrite` is passed explicitly.
- Implemented task-6 launcher as a single zero-arg shell entrypoint at `cluster/run_openvla_maniskill_benchmark.sh`, with strict `set -euo pipefail` sequencing (`check_setup.py` -> `estimate_runtime.py` -> smoke -> full) so setup/estimate failures abort immediately.
- Kept GPU policy enforcement at launcher layer only by exporting `CUDA_VISIBLE_DEVICES=3` by default and passing no GPU-specific logic into shared Python helpers.
- Added one explicit QA-only override `OPENVLA_MANISKILL_VISIBLE_DEVICES_OVERRIDE`; when set (including empty string), it overrides launcher CUDA visibility to deterministically trigger preflight GPU-policy failures without changing default behavior.
- Restored launcher contract to true zero-arg defaults by using `CHECKPOINT_POLICY["fallback_reference"]` when `OPENVLA_MANISKILL_CHECKPOINT` is unset, while keeping strict local-path validation for explicit local checkpoint overrides.
- Final-wave compliance patch: runtime estimator now reports explicit `mode_estimates.smoke` and `mode_estimates.full` and task-wide probes for all `TASK_IDS`, while launcher now runs default post-full exemplar rebake (`rebake_videos.py --run_dir <run_dir>`) and concise docs were updated to match.
